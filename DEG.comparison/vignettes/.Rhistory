install.packages("~/Desktop/DEG.comparison_1.0.tar.gz", repos = NULL, type = "source")
library("DEG.comparison", lib.loc="~/R/x86_64-pc-linux-gnu-library/3.1")
targetspath <- system.file("extdata", "targets.txt", package="DEG.comparison")
targets <- read.delim(targetspath, comment.char = "#")
targets
test <- system.file("extdata", "dataframe_UporDown.R", package="DEG.comparison")
source("test")
system.file
totalgenes <- system.file("extdata", "totalgenes.csv", package="DEG.comparison")
totalgenes <- read.delim (totalgenes, sep=",")
totalgenes
trim.param <- system.file("extdata", "trim.param", package="DEG.comparison")
